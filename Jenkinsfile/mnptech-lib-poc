pipeline {
    agent any
    stages{
        stage("checkout") {
            steps {
                echo "Helloo world"
            }
        }
        stage("Apache status") {
            steps {
                sh "sudo systemctl status apache2"
            }
        }
    }
}
