pipeline {
    agent any
    stages {
        stage("checkout") {
            steps {
                checkout scm
            }
        }
        stage ("Deployment") {
            steps {
                sh "sudo cp -r /var/lib/jenkins/workspace/mnptech-01/* /var/www/html/"
            }
        }
    }
}
