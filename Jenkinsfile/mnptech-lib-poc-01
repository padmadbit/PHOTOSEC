pipeline {
    agent any
    stages{
        stage("checkout") {
            steps {
                echo "Helloo Devops"
            }
        }
        stage("Apache status") {
            steps {
                sh "sudo systemctl status apache2"
            }
        }
    }
}
